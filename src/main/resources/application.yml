server:
  port: 8888

  cloud:
    config:
      server:
        git:
          uri: https://github.com/nhnacademy-be11-1/chaekmate-config-repo.git
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD} #또는 personal access token 사용가능